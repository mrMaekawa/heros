import React from 'react'

import { Header, Sidebar, Character } from '../../'

const LandingPage = () => {
  return (
    <main>
      <Header />
        <Sidebar />
        <Character />
    </main>
  );
};

export default LandingPage;